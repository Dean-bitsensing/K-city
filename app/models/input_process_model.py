from data_structure import *






class InputProcessModel:
    def __init__ (self):
        self.ATM = Atm()

    def initiate(self, file_path):
         self.ATM.
         self.set_h5(file_path)
    def parsing_object():
         
    


def get_h5_datas(self, directory):
    # 폴더 내부의 모든 파일 중 .h5 확장자를 가진 파일을 리스트로 반환
        h5_files = [f'{directory}/'+file for file in os.listdir(directory) if file.endswith('.h5')]
        return h5_files


atms = []

atm = InputProcessModel()
atm.initiate(file_path)

atms.append(atm.ATM)

intersection1 = Intersection

intersection.atm1.scandata.radarobject 